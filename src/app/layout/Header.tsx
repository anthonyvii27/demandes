import { ReactNode } from "react";

const Header = (): ReactNode => (
    <header className="w-full h-[45px] bg-gray-70 border-solid border-b-[1px] border-b-neon-orange flex items-center px-[15px]"></header>
);

export { Header };
