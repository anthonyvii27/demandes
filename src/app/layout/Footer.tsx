import { ReactNode } from "react";

const Footer = (): ReactNode => (
    <footer className="w-full h-[35px] bg-gray-70 border-solid border-t-[1px] border-t-gray-40 flex items-center"></footer>
);

export { Footer };
