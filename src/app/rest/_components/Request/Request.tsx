import { ReactNode } from "react";

const Request = (): ReactNode => (
    <section className="grow border-solid border-l-[1px] border-r-[1px] border-neon-orange">
        Request
    </section>
);

export { Request };
